<h1>Topページ</h1>

<%= search_form_for @q, class:"m-5" do |f| %>

    <%= f.search_field :subject_form_question_cont, placeholder: "キーワードで検索" %>

    <%= f.submit %>
  <% end %>

<div class="container">
    <% @form_questions.each do |form_question| %>
        <div class="card p-3 m-3">
            <div class="list-group list-group-flush">
                <div class="list-group-item">
                    <div class="">タイトル：<%= form_question.subject_form_question %></div>
                    <div class="">質問：<%= form_question.content_form_question %></div>
                </div>
                <div class="list-group-item">
                    <div class="">回答<%= form_question.form_answers.first(1) %></div>
                    <div>
                        <%= link_to "この質問に回答する", new_form_answer_path(form_question_id:
                        form_question.id) %>
                    </div>
                </div>
            </div>

        </div>
    <% end %>
</div>